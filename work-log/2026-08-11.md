# 2026-08-11 工作记录（Skills 接入与独立化）

> 本文件为今日操作的原始流水记录，待后续整理进 CHANGELOG / STATUS / ROADMAP。
> 主题：克隆项目 → 接入小红书 MCP → 把 skills 独立成源真相仓库 → 移动到根目录。

---

## 1. 克隆仓库（根目录 `Travel Guide App/`）

- `git clone git@github.com:heyxiaoze/travel-guide.git` → `travel-guide/`（HEAD `ad9a51d`）
- `git clone git@github.com:heyxiaoze/travel-guide-content.git` → `travel-guide-content/`（HEAD `5067932`）
- 通过 `travel-guide/work-log/`（README / STATUS / CHANGELOG / ROADMAP / SETUP）了解项目来龙去脉：
  - 旅行攻略网站（React + Vite + TS + Tailwind + shadcn/ui），部署在 **Cloudflare Pages**。
  - 游客只读（构建期快照 + 静态兜底）；管理员登录后可站内二次编辑、写回独立的 `travel-guide-content` 仓库并触发重建。
  - Phase 0/1/2 已完成并推送；Phase 3（history / 草稿 GET / 技能上传端点）未开始。
  - 已锁定决策：构建期快照（非运行时 fetch）、内容仓库公开、不支持图片、单管理员、草稿/发布两态、历史版本复用内容仓库 git 历史。
  - ⚠️ 雷区：后台同步工具可能周期性删除 `.git`/`.workbuddy`，动 git 前先 `ls -a`。

## 2. 安装项目内 skills（用户级）

- 源：`travel-guide/.workbuddy/skills/`（`travel-planner`、`travel-guide-addnote`）。
- 安装到 `~/.workbuddy/skills/`（WorkBuddy 全局读取）。
- 安全审计：两 skill 仅含 Markdown + HTML 模板，无脚本/外联/数据外泄，判定 P2 安全。

## 3. 小红书 MCP 调研与接入

- `travel-planner` 原 README 引用的 `jinjunzh/xiaohongshu-mcp` **已 404**，改用活跃维护版 **`xpzouying/xiaohongshu-mcp`**（v2.4.3，Apache-2.0，9k+ stars）。
- 工具名与 skill 调用**完全一致**：`check_login_status` / `get_login_qrcode` / `search_feeds` / `get_feed_detail`。
- 下载 macOS arm64 二进制到 `~/xiaohongshu-mcp/`：
  - `xiaohongshu-login-darwin-arm64`（登录工具）
  - `xiaohongshu-mcp-darwin-arm64`（MCP 服务，首次运行自动下载 ~150MB 无头 Chromium 到 `~/Library/Caches/xiaohongshu-mcp/browser/`）
- 接入 WorkBuddy：`~/.workbuddy/mcp.json` 注册
  ```json
  { "mcpServers": { "xiaohongshu": { "url": "http://localhost:18060/mcp" } } }
  ```
- 登录：`get_login_qrcode` 出二维码 → 小红书 App 扫码 → `check_login_status` 返回 `✅ 已登录`（用户名 `xiaoze🌀木子. 🍜`）。session 缓存在 `~/Library/Caches/xiaohongshu-mcp/`，重启服务可复用。
- ⚠️ 坑：启动**不要**加 `-port=18060`（会被当 listen 地址报 `missing port in address`）；直接不带参数运行，默认监听 `:18060`。
- 实测：`search_feeds` 搜「青海湖 攻略」返回真实笔记（`6a2fe25d…`，赞 511），链路端到端可用。

## 4. 打包 `xiaohongshu-mcp` skill 进项目

- 新建 `travel-guide/.workbuddy/skills/xiaohongshu-mcp/`：`SKILL.md` + `README.md` + `scripts/setup.sh`（按架构下载二进制，已 `bash -n` 校验）。
- 同步安装到 `~/.workbuddy/skills/xiaohongshu-mcp/`。
- 两个 travel skill 的 MCP 依赖说明改为指向该打包 skill（repo 与 installed 副本同步）。

## 5. 建立 skills 独立源真相仓库（第一版，位于 `travel-guide/skills/`）

- 把三套 skill 复制为 `travel-guide/skills/`，独立 `git init`，commit `86fa0fb`。
- 含 `install.sh`（按 `SKILL.md` 同步到目标 `skills/`，默认 `~/.workbuddy/skills`，可传参指定项目内目录）、`README.md`、`.gitignore`。
- `travel-guide/.gitignore` 加 `/skills/` 与 `.workbuddy/skills/`，避免主仓库把独立仓库当嵌套仓库跟踪。
- 已跑 `install.sh` 把三套 skill 同步进 `.workbuddy/skills/`。

## 6. 【本次操作】移动到根目录 + 准备独立 git 仓库

- **移动**：`travel-guide/skills/` → `Travel Guide App/skills/`（同文件系统 `mv`，保留独立 `.git`，commit `86fa0fb` 历史完整）。
- **根目录现状**：`Travel Guide App/` 下含 `skills/`、`travel-guide/`、`travel-guide-content/`、`.workbuddy/`。
- **更新 `travel-guide/.gitignore`**：移除已失效的 `/skills/` 条目（文件夹已移出）；保留 `.workbuddy/skills/`（已安装副本，勿提交）。
- **更新 `skills/README.md`**：反映新位置 `Travel Guide App/skills/`，与主仓库为同级目录、各自独立 git 管理。
- **下一步（用户执行）**：在 `Travel Guide App/skills/` 创建独立 git 仓库并 push（该目录已自带本地 `.git` 与一次初始提交 `86fa0fb`，可直接 `git remote add origin <url>` + `git push -u origin main`）。
- **安装/同步用法不变**：
  ```bash
  bash install.sh                                              # 默认 ~/.workbuddy/skills
  bash install.sh "/Users/heyxiaoze/Library/Mobile Documents/com~apple~CloudDocs/Travel Guide App/travel-guide/.workbuddy/skills"
  ```

---

## 7. 【本次操作】清理 travel-guide 冗余 + skills 仓库准备 push

- **全局 git 身份**：`user.name=heyxiaoze`、`user.email=heyxiaoze@icloud.com`（已 `git config --global`）。
- **清理 travel-guide 废弃内容**：
  - `git rm --cached -r .workbuddy/skills` —— 把主仓库内 vendored 的 skills（6 个文件）移出版本库。源真相已外置到独立仓库 `skills/`，主仓库不再需要副本。**磁盘副本保留**（受 `.gitignore` 忽略），WorkBuddy 仍正常加载，且可随时 `bash install.sh` 重新生成。
  - 校验保留项：`scripts/`（migrate.mjs / sync-content.mjs 仍用于内容外部化）、`upgrade-plan.md`、`work-log/` 均仍相关，未动。
  - `work-log/` 按要求**暂不清理**，仅新增本记录。
- **skills 独立仓库准备 push**：
  - 修正初始提交作者为新的全局身份（`heyxiaoze <heyxiaoze@icloud.com>`，hash `9ee5856`，含 README 位置更新）。
  - `git remote add origin git@github.com:heyxiaoze/skill.git`，分支 `main`。
  - 提交主仓库清理（.gitignore 改动 + 移除 vendored skills + 本 work-log 记录）并 push；skills 仓库 push `-u origin main`。

---

## 关键决策与待办（供整理时参考）

- skills 不参与 Cloudflare 部署（静态 Markdown，仅 WorkBuddy 读取）。
- ✅ 已完成：主仓库 `.workbuddy/skills/` 取消跟踪（vendored 副本移出版本库，磁盘保留）；`skills/` 仓库已修正作者并 push 到 `git@github.com:heyxiaoze/skill.git`。
- 若想彻底从磁盘删除主仓库的 `.workbuddy/skills/`：`rm -rf travel-guide/.workbuddy/skills`（用户级 `~/.workbuddy/skills/` 与独立仓库不受影响）。
